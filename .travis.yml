language: bash


before_install:
  - sudo apt-get update && sudo apt-get install -y shellcheck

script:
        - ls -l
        - bash task1.sh
        - bash task2.sh
        - bash task3.sh
        - ls -l
        - pwd
        - cat file

notifications:
  email:
    on_success: never

